name: Build and upload bundle
on:
  release:
    types: [published]
  push:
  pull_request:
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
      - run: npm clean-install
      - run: npm run build
      - run: node_modules/.bin/esbuild --target=es6 --bundle --minify --outdir=esbuild --global-name=age .
      - uses: actions/upload-artifact@v4
        with:
          name: age-esbuild
          path: esbuild/typage.js
  upload:
    if: github.event_name == 'release'
    needs: build
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: age-esbuild
      - run: gh release upload "$VERSION" "esbuild/typage.js#age-${VERSION#v}.min.js"
        env:
          VERSION: ${{ github.event.release.tag_name }}
          GH_TOKEN: ${{ github.token }}
