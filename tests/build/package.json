{
  "name": "build",
  "private": true,
  "version": "1.0.0",
  "main": "input.js",
  "type": "module",
  "devDependencies": {
    "esbuild": "0.20.1"
  },
  "scripts": {
    "build:release": "npx esbuild --bundle input.js --outfile=age.js --global-name=age"
  },
  "dependencies": {
    "age-encryption": "file:../.."
  }
}
